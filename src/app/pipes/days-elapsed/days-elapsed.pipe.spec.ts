import { DaysElapsedPipe } from './days-elapsed.pipe';

describe('DaysElapsedPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysElapsedPipe();
    expect(pipe).toBeTruthy();
  });
});
