import { DayRelativePipe } from './day-relative.pipe';

describe('DayRelativePipe', () => {
  it('create an instance', () => {
    const pipe = new DayRelativePipe();
    expect(pipe).toBeTruthy();
  });
});
